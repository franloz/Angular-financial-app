export const enum WarningMessages {
  LowerCase = 'Password must contain a lower case letter',
  UpperCase = 'Password must contain a upper case letter',
  Number = 'Password must contain a number',
  SpecialCharacter = 'Password must contain a special character',
  NoSpaces = 'Password must not contain spaces',
  Length = 'Password must be at least 8 characters long',
}
